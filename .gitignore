# dependencies
node_modules/

# build output
dist/
*.tsbuildinfo

# pipeline generated artifacts
pipeline/out/
*.json.gz

# scanjob local state / output (runs on the VM)
scanjob/data/
scanjob/out/
scanjob/venv/
scanjob/testphotos/
scanjob/hashgen-run.log
scanjob/__pycache__/

# server dev-run state (accounts.db lives on the VM in prod)
server/data/

# logs
*.log
npm-debug.log*

# env
.env
.env.*
!.env.example

# local planning notes — never commit (public repo)
notes/

# editor / OS
.DS_Store
Thumbs.db
.vscode/*
!.vscode/extensions.json
.idea/

# vite-plugin-pwa dev artifacts
client/dev-dist/
